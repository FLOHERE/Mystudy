## 실습
- count.py
	- 모음 갯수 세기
	- string의 값을 ch에 하나씩 넣어서 모음이 있는지 검사한다.
	- a,e,i,o,u 중에 하나라도 있으면 카운트 반환
```python
string = input("문자열을 입력하세요 : ")
ch = ""
count = 0

for ch in string:
	if ch in ['a','e','i','o','u']:
	count+=1
print("모음의 개수는 : ", count, "개 입니다.")
```
